/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { UserPipe } from './user.pipe';

describe('Pipe: Usere', () => {
  it('create an instance', () => {
    let pipe = new UserPipe();
    expect(pipe).toBeTruthy();
  });
});