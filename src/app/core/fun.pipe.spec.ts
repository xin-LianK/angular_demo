/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FunPipe } from './fun.pipe';

describe('Pipe: Fune', () => {
  it('create an instance', () => {
    let pipe = new FunPipe();
    expect(pipe).toBeTruthy();
  });
});